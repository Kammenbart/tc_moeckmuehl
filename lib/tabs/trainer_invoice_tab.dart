import 'package:flutter/material.dart';

class TrainerInvoiceTab extends StatelessWidget {
  const TrainerInvoiceTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Trainer – Rechnung (Platzhalter)",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
