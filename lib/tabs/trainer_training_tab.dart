import 'package:flutter/material.dart';

class TrainerTrainingTab extends StatelessWidget {
  const TrainerTrainingTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Trainer – Training (Platzhalter)",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
