import 'package:flutter/material.dart';

class TrainerDunningTab extends StatelessWidget {
  const TrainerDunningTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Trainer – Mahnung (Platzhalter)",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
